# OpenClaw Gateway Environment (Raspberry Pi)
# Deploy to /etc/openclaw/openclaw.env (NOT a local .env file).
# Permissions: 600, owned by openclaw:openclaw
#
# The openclaw user's home is /var/lib/openclaw — NOT /home/pi.
# This enforces identity isolation (ProtectHome=true in systemd).

OPENCLAW_GATEWAY_TOKEN=replace-with-openssl-rand-hex-32
OPENCLAW_GATEWAY_BIND=loopback
OPENCLAW_GATEWAY_PORT=18789
